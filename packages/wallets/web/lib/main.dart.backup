import 'package:flutter/material.dart';
import 'starting_page_widget.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'ATLAS Blockchain App',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: const StartingPageWidget(),
    );
  }
}
